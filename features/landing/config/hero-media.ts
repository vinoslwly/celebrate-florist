/**
 * Path under /public for a real photo of your own bouquet to replace
 * the illustrated flower in the Hero section. Leave `null` until you
 * have one — Hero falls back to the playful SVG illustration.
 *
 * To switch to a real photo: drop the file in `public/illustrations/`
 * (e.g. "hero-bouquet.jpg") and set this to that path. Nothing else
 * needs to change.
 */
export const HERO_BOUQUET_PHOTO: string | null = null;
