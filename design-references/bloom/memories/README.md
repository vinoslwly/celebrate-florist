# Bloom Memories — design references (not deployed)

Founder reference images for living-scene reconstruction (Scenes 4–6 match flow). Keep outside `public/`.

Theme Lab runtime reuses Moments gallery fixtures and shared Bloom assets; no Memories-specific files under `public/themes/bloom/memories/`.
